mod success {
    use fuels::client::FuelClient;
    use fuels::prelude::{Bech32ContractId, ContractId, Provider, WalletUnlocked};
    use fuels::signers::fuel_crypto::SecretKey;
    use node::NetworkPriceProvider;
    use std::env;
    use std::str::FromStr;
    use std::time::Duration;
    use utils::Oracle;
}
